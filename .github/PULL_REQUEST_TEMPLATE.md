Closes #

## Ce que ça fait

## Comment tester

## Lint et build

Colle ici les deux dernières lignes de `npm run lint` et de `npm run build`.

```
```

## Checklist (INT-01, phase 2.1)

- [ ] Seuls `src/features/<lot>/`, `wiki/` et, s'il y a lieu, `src/app/pages/<prenom>.tsx` sont touchés.
- [ ] `Closes #N` est présent ci-dessus.
- [ ] Les deux dernières lignes de `npm run lint` et `npm run build` sont collées ci-dessus.
- [ ] Aucune coordonnée (téléphone, adresse, e-mail) n'apparaît sans `source`.
- [ ] Aucun `any` dans le diff.
- [ ] Je peux expliquer chaque ligne de ce diff.

## Capture (si visuel)
