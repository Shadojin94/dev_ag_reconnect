# `types/` — types partagés

**Propriétaire : rôle Données & Services.**

Les types utilisés par plusieurs parties de l'app (modèles de données, contrats d'API).

Un type qui ne sert qu'à un seul composant reste à côté de ce composant.
