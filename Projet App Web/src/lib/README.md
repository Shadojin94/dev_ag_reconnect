# `lib/` — utilitaires

**Propriétaire : partagé.**

Fonctions pures, sans dépendance à React ni au DOM : formatage de dates, calculs,
validations. Testables en isolation.

Si une fonction a besoin d'un hook React, elle n'est pas ici : elle va dans sa feature.
